.span-15.box
  .left.list
    %span.number.left.clear= "#{item_number}."
    %span.list-item
      %span
        Add a
      = succeed "?" do
        %span.orange Target
      (or
      = "#{item_number + 1})"
  = render 'target'
  = render 'media_source'

